# petergrambook 개발 로그 — 코스모로그

## 프로젝트 개요
- Petergram (인스타) + Peterbook (페북) 로컬 아카이브 뷰어
- 80s Mac UI, SQLite DB, Vanilla JS, 로컬 퍼스트
- 실행: `./start.sh` → localhost:8080

---

## 알게 된 것 (다음에 잊지 말 것!)

### Facebook 백업 구조
- **포스트 HTML에 사진 파일 경로가 있다!** `<img src="your_facebook_activity/posts/media/Photos_xxx/파일명.jpg">`로 참조됨. 처음에 못 잡아서 사진 매칭이 안 됐음
- **사진 파일에 EXIF 날짜 없음** — Facebook이 내보내기할 때 벗겨냄. 파일 수정일도 백업일(2026-03-01)로 통일
- **Facebook 숫자 ID는 대략 시간순** — 완벽하진 않지만 정렬 기준으로 쓸 수 있음
- **메시지 HTML 구조**: `<section class="_a6-g">` 안에 `<h2>`=sender, `<div class="_a6-p">`=text, `<footer class="_a6-o">`=date
- **포스트 HTML 구조**: `<section>` 단위, `<h2>`=title, `<div class="_3-95">`=text, 날짜 패턴 `%b %d, %Y %I:%M:%S %p`
- **message_count 오류**: DB 빌드 스크립트가 HTML 파일 수를 메시지 수로 카운트함. 실제로는 각 파일 안의 `<section>` 수를 세야 함

### Instagram 백업 구조
- **포스트 HTML에 이미지 파일명 직접 포함** — Facebook보다 매칭이 쉬움
- **다중 이미지 포스트**: 같은 `<section>` 안에 여러 `<img>` 태그. 파일명은 개별 숫자 ID라 이름만으로 그룹핑 불가, HTML 파싱 필수
- **스토리 대부분 mp4** — `<img>` 태그로 못 보여줌, `<video>` 태그 필요

### sql.js 제한사항
- **`json_each()` 미지원** — SQLite의 JSON 함수를 sql.js에서 못 씀. JS fallback 필요
- **`file://` 프로토콜에서 DB fetch 불가** — 반드시 로컬 HTTP 서버 필요 (`start.sh`)
- **에러 시 전체 함수 중단** — loadAnalysis 등에 반드시 try-catch 감싸기

### 미디어 경로
- 뷰어 HTML에서 DB의 media_path를 참조할 때 **상대경로 깊이 주의**: `petergram/index.html`에서 `media/` 폴더는 `../media/`(한 단계 위), `../../media/`가 아님
- **symlink는 절대경로로 생성** — 프로젝트 폴더 이동해도 안 깨짐

### UI/UX
- 폰트: pixel 폰트(Geneva/Monaco)는 흐림 → 시스템 폰트(-apple-system)로 변경
- 인스타 포스트 비율: 1:1 아님, 현행 기준 **4:5 (세로형)**
- 스토리: **9:16** 비율, 3열 그리드가 적절 (5열은 너무 작음)
- 디더링 패턴: CSS는 준비됨 (mac-classic.css), JS에서 적용 시 template literal 안에서 변수 스코프 주의

---

## 깨진 이미지 원인 목록

1. **경로 깊이 오류** (`../../` vs `../`) — 수정됨
2. **mp4를 img 태그로 로드** — video 태그로 수정됨
3. **Facebook 사진-포스트 미매칭** — HTML 재파싱으로 3,202건 매칭됨
4. **photos 테이블의 media_path가 peterbook 폴더 기준이 아님** — 수정됨
5. **아직 남은 것**: 일부 포스트의 사진이 photos/mobile/instagram 외 다른 폴더에 있을 수 있음 (cover, character, saelouniyagi 등)

---

## DB 스키마 참고

### petergram.db
- posts: id, date, year, month, caption, media_type, media_path(단일 또는 JSON 배열), hashtags, location
- stories: id, date, year, month, media_type, media_path
- likes, comments, connections, metadata

### peterbook.db
- posts: id, date, year, month, content, media_type, media_path(단일 또는 JSON 배열)
- message_threads: id, thread_name, participant_count, message_count, media_count, last_message_date
- messages: id, thread_id, sender, content, date, media_path
- friends, reactions(reaction_type 컬럼!), ad_interests, search_history, off_meta_activity, photos, albums, metadata

---

## 미완료 / 다음에 할 것
- [ ] Peterbook 깨진 이미지 추가 수정 (cover, character 등 폴더)
- [ ] 메시지 뷰어 페이지네이션 + 날짜 점프
- [ ] 분석 탭 디더링 패턴 안전하게 재적용
- [ ] Phase 5: 증분 업데이트 (Import) 기능
- [ ] 아리공 요청 4~6 (스토리 분류, 크로스포스팅, 긴 댓글)
- [ ] Peterbook 포스트 원문 텍스트 보강 (현재 일부 "📷 사진 게시물"만 표시)
